# Deploys the playground (src/routes → adapter-static SPA) to GitHub Pages on
# every push to main. Separate from ci.yml, which gates PRs and never deploys.
# The Actions-based Pages pipeline (no gh-pages branch): build → upload artifact
# → deploy. `configure-pages` feeds the project-subpath base (`/quillmark-editor`)
# to the build as BASE_PATH; svelte.config.js reads it into kit.paths.base so
# assets and links resolve under the subpath.
name: Deploy playground

on:
  push:
    branches: [main]
  workflow_dispatch:

# `deploy` mints a Pages deployment (pages + id-token); `build` only reads.
permissions:
  contents: read
  pages: write
  id-token: write

# One live deployment at a time; a newer push supersedes a queued one, but a
# running publish is left to finish rather than cancelled mid-flight.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Configure Pages
        id: pages
        uses: actions/configure-pages@v5

      - run: npm ci

      - name: Build playground
        env:
          # `/quillmark-editor` for the project site; svelte.config.js reads it
          # into kit.paths.base.
          BASE_PATH: ${{ steps.pages.outputs.base_path }}
        run: npm run build:app

      - name: SPA deep-link fallback
        # adapter-static emits only the index.html shell (prerender is off).
        # GitHub Pages serves 404.html for any unmatched path, so a hard load of
        # /preview or /visual needs the shell under that name too — the client
        # router then resolves the route. The homepage still gets index.html (200).
        run: cp build/index.html build/404.html

      - uses: actions/upload-pages-artifact@v3
        with:
          path: build

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
