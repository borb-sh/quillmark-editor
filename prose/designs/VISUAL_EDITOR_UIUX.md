# Visual Editor — UI/UX

Scope: the interaction and visual patterns of the VisualEditor — the card-stack
metaphor, per-field affordances, the selection/formatting chrome, the
editor↔preview feel, empty/guidance states, and accessibility. The architectural
composition (what owns what, the address spine, the edit ops) is
[VISUAL_EDITOR.md](VISUAL_EDITOR.md); this doc is the surface a user touches.
Prior art is web-app; the point is which patterns to carry, refine, or drop.

<!-- Skeleton: scope only. Patterns land as we talk each through. -->
