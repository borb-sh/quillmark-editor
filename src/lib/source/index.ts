// `@quillmark/editor/source` — the debug source view (Phase 5).
//
// A read-only CodeMirror surface over `Document.toMarkdown()` — a whole-document
// serialize/parse round-trip, the layer federation deletes, kept for debugging
// only (never an editable dual mode). Lands with Phase 5.
export {};
