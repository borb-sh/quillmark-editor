// `@quillmark/editor/visual` — the WYSIWYG surface (Phase 4).
//
// The federated VisualEditor: `createField` (the vanilla-TS prose leaf over the
// codec) and the `<VisualEditor>` Svelte composition. Depends on `/core` and the
// codec; lands with Phase 4.
export {};
