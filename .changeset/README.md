# Changesets

A change that should ship carries a file here, written by `npm run changeset`: the packages it bumps and the semver level.

The release model is [`prose/canon/RELEASE.md`](../prose/canon/RELEASE.md).
