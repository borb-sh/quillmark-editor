package-lock.json

# Hand-formatted canon and design prose and the reference-quill fixture tree:
# prettier would collapse aligned code-sketch comments and churn a test input.
**/prose/
fixtures/

# Claude Code tooling — agent config and skills copied from the sibling quillmark
# checkout (hand-formatted prose). Never shipped, not package source, so it stays
# out of the prettier gate like `prose/`.
.claude/

# Build output, in every package.
**/dist/
**/build/
**/.svelte-kit/

# Studio's compiled Node half.
packages/studio/bin/
