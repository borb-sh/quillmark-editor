# Document Model

Scope: the shared substrate both surfaces depend on — the live `Document` as
source of truth, the `@quillmark/wasm` + quiver boundary (init, handle ownership,
render sessions), and diagnostics.

Corpus content types (`RichText`) and their ProseMirror mapping live in
[CODEC.md](CODEC.md).

<!-- Skeleton: scope only. Content pending discussion. -->
